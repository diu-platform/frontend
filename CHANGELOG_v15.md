# 📝 DIU Physics Interactive v15.13 — Changelog
## Transmission Efficiency & Quantum Statistics

**Дата:** 21 декабря 2025  
**Основа:** r3f-v15.10  
**Источник:** Optica Publishing Group (opg.optica.org)

---

## 🆕 Новое в v15.13

### 1. Transmission Efficiency (Потери на барьере)
- Новый параметр "Transmission" (50-100%)
- Частицы с вероятностью (100-T)% поглощаются/отражаются
- Счётчик потерянных частиц в статистике
- Доступен в Lab режиме (скрыт в Demo)

### 2. Statistical Confidence (Статистическая достоверность)  
- Индикатор уверенности: C = 1 - 1/√N
- Погрешность: σ/√N в процентах
- Цветовая индикация по количеству частиц
- Квантовая заметка при малом N

### 3. Квантовая статистика в TheorySection
- Правило Борна: P(x) = |Ψ(x)|²
- Формула достоверности: C = 1 - 1/√N
- Объяснение статистической природы паттерна

### 4. UI улучшения (v15.11-v15.12)
- Reset теперь полностью очищает данные (через React key)
- 6 ротирующихся подсказок (каждые 5 сек)
- Кнопка "Points" скрыта в Demo режиме
- Расширенный tooltip для толщины барьера

### 5. Исправления
- Исправлен `continue` → `return` в forEach (v15.13)
- Исправлена структура кода в Particles.tsx

---

# 📝 DIU Physics Interactive v15.10 — Changelog
## Barrier Thickness & Full Documentation

**Дата:** 19 декабря 2025  
**Основа:** r3f-v15.9  
**Источник:** Optica Publishing Group (opg.optica.org)

---

## 🆕 Новое в v15.10 (относительно v15.9)

### 1. Параметр толщины барьера (Barrier Thickness)
- Новый параметр `t` — толщина барьера (0.02-0.5 мм)
- **Физика:** θ_max = arctan(a/t) — максимальный угол прохождения
- **Эффект:** Толстый барьер работает как коллиматор
- Доступен в Lab и Research режимах
- Скрыт в Demo режиме (фиксированное значение 0.1 мм)

### 2. Обновлённая формула интенсивности
```
I(θ) = I₀ · V · cos²(πd·sinθ/λ) · sinc²(πa·sinθ/λ) · G(θ) · A(θ)
```
Где A(θ) — angular cutoff function с плавным переходом

### 3. Документация толщины барьера
- **TheorySection:** Добавлен в ключевые параметры + формула θ_max
- **ExperimentGuide:** Подсказки и рекомендации на RU/EN
- **InlineDocumentation:** Полная документация с источниками
- **ControlsPanel:** Tooltip на 8 языках
- **README.md:** Обновлённое описание формулы

### 4. Визуальные изменения
- Толщина барьера в 3D масштабируется: 0.02→тонкий, 0.5→толстый
- Оранжевый слайдер в Lab режиме
- Предупреждение при толстом барьере (>0.3 мм)

---

# 📝 DIU Physics Interactive v15.1 — Changelog
## Full Optica-Based Scientific Improvements

**Дата:** 18 декабря 2025  
**Основа:** r3f-v14-lab-mode  
**Источник:** Optica Publishing Group (opg.optica.org)

---

## 🆕 Новое в v15.1 (относительно v15.0)

### 1. Параметр когерентности источника
- Диапазон: 0-100%
- Влияет на видимость (контраст) интерференционных полос
- При 0% — полностью некогерентный свет, интерференция исчезает

### 2. Гауссов профиль пучка
- Добавлена огибающая G(z) = exp(-2(z/w)²)
- Реалистичное затухание к краям паттерна
- Настраиваемая ширина пучка

### 3. Компонент TheoryComparisonOverlay
- Наложение теоретической кривой на экспериментальные данные
- Расчёт R² (коэффициент детерминации)
- Цветовая индикация качества совпадения

### 4. Расширенная статистика
- Mean (среднее положение)
- StdDev (стандартное отклонение)
- Skewness (асимметрия)
- Kurtosis (эксцесс)

---

## 🔬 Научные улучшения

### 1. Полная формула интерференции (Fresnel-Kirchhoff)

**Файл:** `simulations/DoubleSlit/hooks/useInterference.ts`

**Было (v14):**
```typescript
// Упрощённая формула
const probability = Math.pow(Math.cos(phaseDiff / 2), 2);
```

**Стало (v15):**
```typescript
// Полная формула Френеля-Кирхгофа
// I(θ) = I₀ · cos²(πd·sinθ/λ) · sinc²(πa·sinθ/λ)

// Интерференционный член (от двух щелей)
const interferencePhase = Math.PI * d * sinTheta / lambda;
const interference = Math.pow(Math.cos(interferencePhase), 2);

// Дифракционная огибающая (от ширины щели)
const diffractionPhase = Math.PI * a * sinTheta / lambda;
const sinc = diffractionPhase === 0 ? 1 : Math.sin(diffractionPhase) / diffractionPhase;
const envelope = Math.pow(sinc, 2);

// Итоговая вероятность
const finalProb = interference * envelope;
```

**Источник:** Pearson et al. 2018 "Measurements of slit-width effects in Young's double-slit experiment"

---

### 2. Новый параметр: Ширина щели (slitWidth)

**Файлы:** 
- `simulations/DoubleSlit/index.tsx`
- `simulations/DoubleSlit/Particles.tsx`
- `components/ControlsPanel.tsx`
- `i18n/translations.ts`

**Добавлено:**
- Параметр `slitWidth` (диапазон: 0.01 - 0.2 mm)
- Слайдер в панели управления
- Влияние на огибающую паттерна (sinc² term)

**Физика:**
| Ширина щели | Эффект |
|-------------|--------|
| Узкая (0.01 mm) | Широкая огибающая, много полос |
| Широкая (0.2 mm) | Узкая огибающая, мало полос |

---

### 3. Дискретная детекция фотонов

**Файл:** `simulations/DoubleSlit/Particles.tsx`

**Было (v14):**
```typescript
// Плавное затухание
meshMat.opacity -= dt * 1.5;
```

**Стало (v15):**
```typescript
// Мгновенная яркая вспышка при детекции
// На основе: "binary assembly of 0 and 1 photon counts" (Optica)

if (!p.hitFlashDone) {
  meshMat.color.setHex(0xffffff); // Белая вспышка
  mesh.scale.setScalar(sizeScale * 1.8); // Расширение
  glow.scale.setScalar(sizeScale * 2.5);
  p.hitFlashDone = true;
}

// Быстрое затухание (80ms)
const flashDuration = 0.08;
```

**Источник:** Hong & Noh 1998 "Two-photon double-slit interference"

---

### 4. Визуализация точек накопления

**Файл:** `simulations/DoubleSlit/HeatmapScreen.tsx`

**Добавлено:**
- Отдельные точки детекции на экране
- Накопление до 500 точек
- Постепенное затухание старых точек
- Переключатель `showDiscretePoints`

**Физика:**
> "diffraction pattern arises as a mosaic point by point from clicks of single-particle detectors" — Optica

---

## 🎛️ Новые элементы UI

### ControlsPanel.tsx

| Элемент | Описание |
|---------|----------|
| Слайдер "Slit Width" | Ширина щели 0.01-0.2 mm |
| Кнопка "Points" | Вкл/выкл дискретных точек |
| Индикатор Lab Mode | Визуальное обозначение режима |

### Translations

```typescript
slitWidth: { ru: 'Ширина щели', en: 'Slit Width' }
narrower: { ru: 'Узкая', en: 'Narrow' }
wider: { ru: 'Широкая', en: 'Wide' }
points: { ru: 'Точки', en: 'Points' }
labMode: { ru: 'Лабораторный режим', en: 'Laboratory Mode' }
```

---

## 📁 Изменённые файлы

| Файл | Изменения |
|------|-----------|
| `hooks/useInterference.ts` | Полная формула, sinc², slitWidth |
| `Particles.tsx` | Вспышка детекции, slitWidth prop |
| `HeatmapScreen.tsx` | Дискретные точки накопления |
| `index.tsx` | Новые параметры, обработчики |
| `ControlsPanel.tsx` | Слайдер ширины, кнопка точек |
| `translations.ts` | Новые переводы |

---

## 🧪 Тестирование

### Проверить:

1. **Интерференционная картина**
   - [ ] Изменение ширины щели влияет на огибающую
   - [ ] Паттерн соответствует теоретическому

2. **Детекция фотонов**
   - [ ] Яркая вспышка при попадании
   - [ ] Быстрое затухание (~80ms)

3. **Точки накопления**
   - [ ] Дискретные точки на экране
   - [ ] Накопление паттерна "точка за точкой"

---

## 📚 Ссылки на источники

1. **Pearson et al. 2018** — sinc² формула
   https://opg.optica.org/osac/abstract.cfm?uri=osac-1-2-755

2. **Hong & Noh 1998** — бинарные 0/1 клики
   https://opg.optica.org/josab/abstract.cfm?uri=josab-15-3-1192

3. **Time-resolved interference** — точка за точкой
   https://opg.optica.org/abstract.cfm?uri=IQEC-2013-IA_3_1

---

*DIU Physics Interactive v15*  
*Научная точность на основе Optica Publishing Group*
